# Demo credentials only. Replace stringData values (or better, manage this
# secret outside git) before using this anywhere beyond a local minikube demo.
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: statuspulse
type: Opaque
stringData:
  POSTGRES_DB: tasksdb
  POSTGRES_USER: taskuser
  POSTGRES_PASSWORD: taskpass
